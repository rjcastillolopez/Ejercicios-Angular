import { BorderResultDirective } from './border-result.directive';

describe('BorderResultDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderResultDirective();
    expect(directive).toBeTruthy();
  });
});
